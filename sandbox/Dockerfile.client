FROM ubuntu:22.04

RUN apt-get update && apt-get install -y \
    iputils-ping \
    traceroute \
    dnsutils \
    curl \
    net-tools \
    iproute2 \
    iptables \
    && rm -rf /var/lib/apt/lists/*

CMD ["sleep", "infinity"]